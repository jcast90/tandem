{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Tandem desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open", "websocket:default"]
}
